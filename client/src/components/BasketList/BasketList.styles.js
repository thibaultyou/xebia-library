import styled from 'styled-components';

export const Cover = styled.img`
  height: 64px;
`;

export const styles = theme => ({
  root: {
    width: '100%',
    overflowX: 'auto',
  },
  table: {
    minWidth: 500,
  },
  removeButton: {
    margin: 5,
  },
});
